const constants = {
    BASE_URL: __dirname + "\\src",
    BASE_STATE: `${__dirname}\\src\\assets\\state`,

    OUTPUT_FOLDER: `${__dirname}\\src\\assets\\output`,
    ASSETS_FOLDER: `${__dirname}\\src\\assets`,
}

export {
    constants
}